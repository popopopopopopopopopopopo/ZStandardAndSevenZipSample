<Window x:Class="ZStandardNETSample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525">
    <StackPanel>
        <Button Command="{Binding CompressCommand}" Content="ZStandard圧縮"></Button>
        <Button Command="{Binding DeCompressCommand}" Content="ZStandard解凍"></Button>
        <Button Command="{Binding SevenzipArchiveCommand}" Content="7zip圧縮"></Button>
        <Button Command="{Binding SevenzipDeArchiveCommand}" Content="7zip解凍"></Button>
    </StackPanel>
</Window>
